// Literals are fixed values that we insert directly into our code. They can be of different types, such as numbers, strings, booleans, arrays, and objects.

let name ="Raj" ;
let age =35;
let company = "Experian Hyderabad"

console.log(`My name is ${name} and I am ${age} years old and I work at ${company}.`); // Using template literals to insert variables into a string

